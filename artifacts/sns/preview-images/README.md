# Preview Images

Use the production Open Graph image at `/og-agentproof.png` unless a channel-specific preview is approved later.

Verification required after deployment:

- Role-specific pages show the correct title and description.
- Preview image is not cropped in a way that hides the product signal.
- Mobile preview is readable.
- Canonical URL points to the role-specific survey page.
